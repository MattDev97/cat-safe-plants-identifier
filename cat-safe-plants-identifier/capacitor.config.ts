import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Cat Safe Plants Identifier',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
